!!! 5
%html
  %head
    %title WaveCycle
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    %meta(name="apple-mobile-web-app-capable" content="yes")
    %meta(name="viewport" content="user-scalable=no, width=device-width, initial-scale=1.0, maximum-scale=1.0;")
  %body
    %h1
      WaveCycle
      %small An online single-cycle waveform generator
    .container-fluid
      = yield
/ Sawtooth (25 harmonics)
/  Math.sin( Math.sin(radians)+Math.sin(radians*2)/2+Math.sin(radians*3)/3+Math.sin(radians*4)/4+Math.sin(radians*5)/5+Math.sin(radians*6)/6+Math.sin(radians*7)/7+Math.sin(radians*8)/8+Math.sin(radians*9)/9+Math.sin(radians*10)/10+Math.sin(radians*11)/11+Math.sin(radians*12)/12+Math.sin(radians*13)/13+Math.sin(radians*14)/14+Math.sin(radians*15)/15+Math.sin(radians*16)/16+Math.sin(radians*17)/17+Math.sin(radians*18)/18+Math.sin(radians*19)/19+Math.sin(radians*20)/20+Math.sin(radians*21)/21+Math.sin(radians*22)/22+Math.sin(radians*23)/23+Math.sin(radians*24)/24+Math.sin(radians*25)/25 )

